<%= form_with(model: @task, local: true, url:confirm_taskss_path) do |form| %>
<% if @task.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@task.errors.count, "error") %> prohibited this <%= @task.name %> from being saved:</h2>
  <ul>
  <% @task.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :タスク名 %>
  <%= form.text_field :title %>
</div>

<div class="field">
  <%= form.label :内容 %>
  <%= form.text_field :to_do %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>
